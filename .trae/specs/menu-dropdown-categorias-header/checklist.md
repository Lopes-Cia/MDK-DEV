- [ ] O header desktop exibe um dropdown “Todas as categorias” alimentado pelo `produtos-store`.
- [ ] O dropdown navega usando `categoria.slug` (path completo) e não monta slug manualmente.
- [ ] Estados de loading/empty/error aparecem no dropdown sem quebrar o header.
- [ ] Nenhum componente de UI chama API diretamente; o carregamento vem via store.
- [ ] Se `@radix-ui/react-dropdown-menu` for adicionado, a mudança foi aprovada e o build continua funcionando.

